[project]
name = "agent-builder-agent-runner"
version = "0.1.0"
description = "AgentBuilder Agent Runner"
requires-python = ">=3.12"
dependencies = [
    "celery[redis]==5.4.0",
    "redis==5.2.1",
    "fastapi==0.115.6",
    "uvicorn[standard]==0.34.0",
    "pydantic-settings==2.7.1",
    "asyncpg==0.30.0",
    "structlog==24.4.0",
    "minio==7.2.13",
    "cryptography==44.0.0",
    "claude-agent-sdk==0.2.144",
]

[dependency-groups]
dev = [
    "pytest==8.3.4",
    "pytest-asyncio==0.25.1",
    "httpx==0.28.1",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]
